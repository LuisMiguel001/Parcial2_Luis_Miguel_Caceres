@page "/C_Entradas"
@inject HttpClient httpClient

<div class="card">
	<div class="card-header">
		<h3>Consulta de Entradas</h3>
	</div>

	<div class="card-body">
		<div class="row">
			<button type="button" @onclick="Buscar" class="btn btn-primary input-group-text">
				<i class="oi oi-magnifying-glass " />
			</button>
		</div>
		@if (entrada is not null)
		{
			<table class="table table-striped table-hover">
				<thead>
					<tr>
						<th>ProductoId</th>
						<th>Fecha</th>
						<th>Concepto</th>
						<th></th>
					</tr>
				</thead>
				<tbody>
					@foreach (var entrada in entrada)
					{
						<tr>
							<td>@entrada.ProductoId</td>
							<td>@entrada.Fecha.ToString("dd/MM/yyyy")</td>
							<td>@entrada.Concepto</td>
							<td><a href="Producto/@entrada.ProductoId">Ver</a></td>
						</tr>
					}
				</tbody>
			</table>
		}
	</div>

	<div class="card-footer">
		<div class="form-row justify-content-sm-between" aria-orientation="vertical">
			<div class="col-3">
				<label for="CantidaRegistro" class="col-form-label"><strong>Filas</strong>&nbsp;</label>
				<input id="CantidaRegistro" @bind-value="@conteo" disabled class="form-control" />
			</div>
		</div>
	</div>
</div>

@code {
	public List<Entradas>? entrada { get; set; }
	public int conteo { get; set; }

	public async Task Buscar()
	{
		entrada = await httpClient.GetFromJsonAsync<List<Entradas>>($"api/Entradas");

		conteo = entrada != null ? entrada!.Count() : 0;
		StateHasChanged();
	}
}
